# CLAUDE.md — note コンテンツ制作チーム(運用ルール)

このリポジトリは、**note.com** で新規ブランドのコンテンツを展開する AI エージェント・チームを定義する。

> ⚠️ **秘匿情報(note の資格情報・メール・トークン)はコミットしない。** リポジトリの公開/非公開に関わらず徹底する。

## このリポジトリについて
- これは [hyakushiki-00100/NOTE](../NOTE)(Zen Kyu ブランド)・[hyakushiki-00100/NOTE2](../NOTE2)(すきま時間ラボ、子育て支援制度の調査ブランド)
  とは**別アカウント・別ブランド**のための制作チーム。
- **引き継いだもの**: note.com の運用ルール・プラットフォーム制約・品質基準・エージェント構成(NOTE2 での運用で磨いた版)。
- **引き継いでいないもの**: ブランド名・著者名・話者ペルソナ・配色・商品設計(調査型か体験談型か)・既出テーマなど、
  NOTE / NOTE2 固有の内容は一切含まない。**ジャンルも子育て支援制度に限定しない**(NOTE2 での検証で、このジャンルは
  有料化できるネタが早期に枯渇し、イクハク等の構造的競合が強いことが分かっている。別ジャンルから検討してよい)。
- **ブランド / 読者 / トーン / 商品設計**は、このセクションに方向性が決まり次第、追記する(現時点では未定義)。

## ブランド / 読者 / トーン / 商品設計(決定事項・2026-08-10)
- ブランド名: (検討中。著者名は確定)
- 著者名: **カズマ**(ペンネーム)
- 商品設計: **制作物提供型**(AIプロンプト集・Canvaテンプレート等)。書き手の体験談ではなく、
  エージェントチームが実際に検証しながら作った制作物そのものが商品。
  記事のフックは「私はこう稼いだ」ではなく「この制作物はこう使うと良い」で語る(実績訴求はしない)。
  業種は絞らず商品(プロンプト/テンプレート)は共通、届け方(記事)を業種別に変えてリーチする
  (例:「〇〇業のためのChatGPTプロンプト」のように業種ごとに記事を展開する)。
- 書き手: 実在の人物。**40代・男性**。本業のかたわらAIを使った副業(このプロジェクト)に挑戦中。
  収益実績は未確立であり、確立した実績があるかのように書かない(「挑戦中」「模索中」という現在進行形の立場で書く)。
- 想定読者: AIプロンプト・テンプレートを実務で使いたい人。業種別記事を通じて各業種の実務者にリーチする。
- トーン: 誠実・地に足のついた・等身大(体験を盛らない、副業に挑戦中の立場のまま語る)。
- 配色(暫定・オーナー未最終確認): ベース オフホワイト `#F4F1EC` / メイン スレートネイビー `#33465C` /
  アクセント 琥珀 `#D99B45` / サブ ライトグレー `#B9BFC7`。NOTE2 のテラコッタ系とは意図的に区別する
  (AI・実務ツールという題材に合わせ、落ち着いた寒色寄りの配色にした)。オーナーの意向があれば変更する。
- プロフィール:
  - Bio: 「カズマ。40代男性。本業のかたわら、AIを使った副業を模索中。ここでは、自分で試して効果のあったAIプロンプトやCanvaテンプレートを紹介しています。」
  - アイコン: 書き手の顔を模さない、抽象的なイラスト(`cover-maker` が作成)。

### 着手前に確認したこと(NOTE2 の経緯からの申し送り・確認結果)
- **書き手の実際の属性・実体験**: 40代男性、カズマ(ペンネーム)。AIを使った副業に「挑戦中」という現在進行形の立場。
  収益実績・専門資格などの確立した実績は無い前提で運用する(捏造禁止)。
- **一次情報へのアクセス制約**: 検証済み。`WebFetch` は note.com・政府機関ドメインに限らず、Wikipedia 等の
  一般ドメインを含めほぼ全ドメインが `EGRESS_BLOCKED`(ホワイトリスト方式と推定)。`WebSearch` は機能し、
  検索結果のスニペット経由で一次情報(mhlw.go.jp・e-stat.go.jp 等)の URL・要旨は拾えることを確認した。
  **全文の精読はできない制約がある**ため、出典表記は「◯◯(検索結果の要旨で確認、全文未読)」等、実態に即して書く。
- **ジャンル**: 「調べて要約する」型から「制作物(プロンプト/テンプレート)を作って売る」型に転換したため、
  この論点(AI検索による価値低下)は該当しない。ただし新しい懸念として、AIプロンプト集・Canvaテンプレートは
  PromptBase・Gumroad 等で供給過多になりやすいジャンルであるため、差別化は
  「業種別の具体性」と「実際に検証したものだけを載せる」で図る方針とした(継続して `marketing-strategist` で検証)。

## 記事の作り(運用ルール)
- **note 用に新規執筆する**(他媒体からの転載にしない)。短い段落・問いかけを増やす。
- 構成パターンはテーマに応じて設計するが、**事実/推測の区別は「補足:事実関係の整理」節を作らず、地の文に溶かす**
  (「〜と言われています(諸説あるそうですが)」「ここは私の解釈です」)。低確信の接続は明示する。

### 「AIっぽさ」を出さない
- AI が書いたように見えると、「人が調べて書いた」という価値が落ちる(誰でも出せる内容に見えるため)。詳細な禁止表現と
  検査コマンドは `.claude/agents/writer.md` に定義し、`note-qa` が投稿前に grep で検査する。
- **「断定しない」は出典の無い事実主張に対する制約であって、文章全体をぼかす指示ではない。**
  出典がある事実は言い切る。自分の判断は自分の考えとして言い切る。出典が無いことは「確認できませんでした」と書く。
  この3つを混同すると、語尾が全部「〜と言えるでしょう」になり、最も AI らしい文章になる。

### 出典の扱い
- **末尾に「参照した情報」節を置き、媒体名と URL を列挙する。**地の文で確からしさを示すルールと併用する
  (地の文=読みやすさ、末尾リスト=検証可能性)。
- **一次情報を優先する。**公式発表・法令・統計が取れるものは必ず当たる。取れなかった場合は
  「一次情報は確認できませんでした」と本文に書く。
- **利益相反のあるソースに依拠する場合は、その立場を明記する。**
  (例: そのサービスを販売している側の記事、アフィリエイトサイト、ノウハウ販売者の発信)
- 出典を示せない事実主張は**書かない**。数値は特にそうする。
- 分量目安は記事の位置づけで決める(無料公開は軽め、有料は厚め)。**字数は目視でなくコードで確認する**:
  ```bash
  python3 -c "import sys,re;t=open(sys.argv[1]).read();b=''.join(l for l in t.splitlines() if not l.lstrip().startswith(('#','※')));print('chars:',len(re.sub(r'\s','',b)))" article.md
  ```

## note.com プラットフォーム制約(実測・投稿形式に直結)
- **HTML 直貼り不可**(リッチテキストのため HTML は文字列表示)。投稿用は **note 対応 Markdown**(**表→箇条書き**)。
- **見出しは h2 / h3 の 2 段階のみ**、サイズは note テーマ固定。大見出しは `##` に統一。
- **タイトルは自動で大表示**(記事側で制御不可)→ 本文と分けて【タイトル案】で明記。
- **改行は制御可**: 3 文以上の段落は ~2 文で分割し空行を挟む(モバイル可読性)。
- 完全なレイアウト制御が要るなら、note でなく自社サイト用の独立 HTML にする。

## 有料記事の設計(有料公開する場合)
- **有料価値の中身は商品設計によって決まる**(調査型なら網羅性と検証の深さ、体験談型ならその人ならではの経験、
  制作物提供型ならプロンプト/テンプレート本体の質と量、等)。**書き手の実績を偽って有料価値を作らない。**
  - 無料エリア: 制作物の一部サンプル・使い方の考え方・対象業種の提示
  - 有料エリア: プロンプト/テンプレート本体一式・業種別のカスタマイズ例・検証時に分かった注意点(制作物提供型の場合の例)
- 無料エリアだけで独立した価値が要る(フック・問題提起・要点の一部)。有料ラインの直前で「この先に何があるか」を
  具体的に予告する(煽り文句ではなく中身の予告)。無料で約束したものは有料で必ず渡す。
- 有料ラインは `<!-- ▼ ここから有料 ▼ -->` のように、貼り付け時に見分けられる形でメモを残す。
- **価格は断定せず**、note で類似記事の価格を確認して最終決定するよう案内する(実データを持たないため)。

## ハッシュタグ(日本語)
- note は**日本語ハッシュタグ**が主流。シリーズ共通枠 + 記事固有枠の組み合わせを提案する。
- **note 検索画面で各タグの投稿数を確認**して最終決定する(実データを持たないため断定しない)。

## カバー画像
- note 推奨比は **1280×670**(書籍カバー等の 1280×720 とは違うので混同しない)。
- サイズは目視でなく**コードで検証する**:
  ```bash
  python3 -c "from PIL import Image;print(Image.open('cover.png').size)"
  ```

## エージェント構成(推奨・汎用)
| エージェント | 役割 |
|---|---|
| `marketing-strategist` | 需要・競合の調査と記事テーマの選定(ソースの利益相反を必ず確認する) |
| `writer` | 本文執筆(制作物提供型。出典を明記し、実体験を捏造せず、AIっぽい言い回しを避ける) |
| `viral-analyst` | タイトル・フック・構成の評価(実績訴求型のフックは提案しない) |
| `note-formatter` | note 制約への整形(h2/h3・表→箇条書き・短段落・【タイトル案】) |
| `hashtag-strategist` | 日本語ハッシュタグ設計と投稿数の確認手順 |
| `cover-maker` | カバー画像・アイコンの作成/リサイズ(サイズと縮小時の視認性をコード検証) |
| `note-monetizer` | 有料記事の無料/有料の切れ目・価格・購入導線の設計(有料価値はプロンプト/テンプレート本体) |
| `note-qa` | 実体験の捏造ゼロ・出典の明記・確信度ラベル残存ゼロ・重複・字数の確認 |

## 非交渉の品質基準
- **書き手が経験していないことを、経験したように書かない。**実績・収入・体験・資格の捏造は禁止。
  商品設計(調査型・体験談型・制作物提供型のいずれでも)に関わらず最優先の基準とする。
- **出典を示せない事実主張は書かない。**示せない場合は「確認できませんでした」と本文に書く。
- **利益相反のあるソースは立場を明記する**(販売者の発信・アフィリエイトサイト等)。
- 出典の裏取り。**確信度のラベルを本文に残さない**(確からしさは地の文で)。本文に商用製品名を書かない。
- 事実性が不確かな由来・語源を扱う場合は、本文で出自・裏取り状況を明記する。
- 既出テーマ(`articles/` 内の公開済み記事)と重複しない切り口を選ぶ。

## 動作規範(要約)
着手前に要件を構造化 / 完了前に自己検証(字数・画像サイズ等はコードで) / 出力は省略しない /
事実と推測を混ぜない / 根本原因対応 / 結論を先に・淡々と。オーナー報告・記事とも日本語。
